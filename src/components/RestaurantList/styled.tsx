import styled from 'styled-components';

const St = {
  Layout: styled.ul`
    display: flex;
    flex-direction: column;

    height: calc(100vh - 158px);
    padding: 0 16px;

    overflow: scroll;
  `,
};

export default St;
